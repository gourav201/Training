apiVersion: v1
kind: Service
metadata:
  name: {{ include "sechelm.fullname" . }}
  labels:
    {{- include "sechelm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sechelm.selectorLabels" . | nindent 4 }}
